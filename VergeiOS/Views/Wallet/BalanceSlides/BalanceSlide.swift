//
//  BalanceSlide.swift
//  VergeiOS
//
//  Created by Swen van Zanten on 04-08-18.
//  Copyright © 2018 Verge Currency. All rights reserved.
//

import UIKit

class BalanceSlide: UIView {

    var backgroundView: UIView?

    override func draw(_ rect: CGRect) {
        super.draw(rect)

        /*
        let layerRect = CGRect(x: rect.origin.x + 20, y: rect.origin.y, width: rect.width - 40, height: rect.height)
        backgroundView = UIView(frame: layerRect)
        backgroundView?.backgroundColor = UIColor.white.withAlphaComponent(0.05)
        backgroundView?.layer.cornerRadius = 5

        addSubview(backgroundView!)
        */
    }
}
